import React from 'react';
import PageLinks from '../constants/pageLinks';

export default () => {
    return (
        <nav id="menu" className="menu">
            <PageLinks />
        </nav>
    );
};
