import React from 'react';
import Layout from '../components/layout';
import Calendar from '../components/calendar';

export default () => {
    <Layout fullMenu>
        <Calendar />
    </Layout>;
};
